/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SimonSaysMain from './SimonSaysMain';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SimonSaysMain);
